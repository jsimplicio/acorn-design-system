# Hide everything by default
*

# But keep these essential files
!README.md
!.gitignore
!.github/
!.github/**

# Keep the src directory and all its contents
!src/
!src/**

# Keep the dist directory (built CSS and utils)
!dist/
!dist/**

# Keep the examples directory
!examples/
!examples/**

# Keep package.json and package-lock.json for npm compatibility (if users want to build)
!package.json
!package-lock.json

# Keep the src/ directory with split token files
!src/
!src/**

# Keep the build configuration for the acorn design system
!tokens-config.cjs

# Keep the components directory with all web components
!components/
!components/**

# Keep the demo page
!demo.html
